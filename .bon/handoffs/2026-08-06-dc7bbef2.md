# Handoff — 2026-08-06

session_id: dc7bbef2-b569-4fa5-8438-8e6f6fb49084
purpose: Worked the tgt board to empty: shipped the 5xx retry fix (tgt-fanoki), credential-level eval isolation (tgt-wisuwe), and the house-convention eval cell (tgt-sogude); suites 1.35.3 + 1.35.4
format: fond-v1

## For the next Claude

### Done
- Closed tgt-fanoki under tgt-radaji: transport-layer retry in accomplis (429/503 all methods, 500/502/504 idempotent-only — a replayed POST could double-create; 503 is pre-processing load-shed). Stub-proven per the brief (503-503-200 succeeds in 3 calls, 401 fails on the first). Shipped suite 1.35.3. Also rewrote a test whose `httpx.get` mock had stopped matching and was silently making REAL network calls.
- Closed tgt-wisuwe: eval runs now execute as the `mitester` user — kernel wall (/home/modha is 700), staged inputs, sudo crossing, scoped Vertex SA (`eval-mitester@itv-mit-llm-sameer`, aiplatform.user on the pot only). `containment-check.sh` is the standing falsifier: 16/16, after its own first run exposed a vacuous green and gained the evidence gate (a sabotage run counts only when cli-calls.log proves the shim ran). EVAL_KEEP=1 retrieves inner transcripts.
- Closed tgt-sogude: world-c + review-tier-c, the cell only convention can win. Deterministic verdict from final world state (n=3/cell): skill lands the 3-5 tier 9/9 at every model; bare diverges 7/9, always urgency-led (tier collapses to the two dated cards; bare Opus part-infers 2/3). Inventory intact all 18 runs. Judge unreliable both directions — hard anchors added for future grades; deterministic check is the cell's verdict. Bonus catch: skill now names the UI/API priority inversion (UI P1 = `--priority 4`), shipped suite 1.35.4.
- todoist-sync (fade539): ⏳ retry lines now pass through to the journal on SUCCESS — without this, a cleanly-absorbed wobble left no trace and tgt-radaji's verify was unmeasurable.
- Removed the `todoist-gtd -> accomplis` compat symlink (2 Aug rename residue; its own doc said the next session removes it; nothing referenced it).
- Filed banc-jicuzu (smevals `grade` needs task scoping; upstream is fast-moving — check before building). Wrote a memory file: Planet Modha's two Claude logins share ONE org spend pool.

### Reflection
The session's recurring shape was instruments catching their own blindness — three times. A CLI test's dead mock degraded into live network calls; the containment check's first run passed its sabotage greps vacuously off an auth failure (no model ran, so no markers and "fail" appeared in the error text); and the LLM judge zeroed deterministically-perfect runs, one grading note conceding the run "avoids the 0.0 floor" then scoring 0.0 anyway. Every fix was the same move: gate the verdict on proof the subject actually executed. Sameer's steer to check the tgt list beat my instinct to hunt papercuts estate-wide, and his "smevals is third-party, banc is our wrapper" re-routed the field report before it landed on the wrong board.

### Uncertain
- Bare models' tier-of-2 is mechanism-solid (urgency-led reasoning, hand-read) but count-coupled to the fixture: world-c has exactly two dated cards. More or fewer dated cards would move the bare numbers; the divergence claim rests on the register, not the 2.
- The escape mechanism from 2026-08-05 remains unproven between its two candidates; the kernel wall closes both, so it's moot for safety but still unknown forensically.

### Risks
- The 18 review-tier-c judge grades in the record predate the hard anchors and are unreliable in BOTH directions — `smevals report -g judge` mixes them in silently. README documents this; a targeted regrade is blocked on banc-jicuzu.
- After Thursday's overnight reset un-caps the Planet Modha org, an eval sweep driven from a NON-Vertex session bills mitester's own subscription login — same shared family pool that just ran dry. By design, but a big sweep would drain it again; drive sweeps from a Vertex session (the runner then uses the scoped SA).
- The eval SA key is a standing no-expiry credential in mitester's home — blast radius is pot model-spend only (no data). Revoke with one `gcloud iam service-accounts keys delete` if ever in doubt.
- mitester's claude is 2.1.207 (modha: 2.1.223). If eval runs start failing on flags/aliases, update it as mitester before debugging anything else.

### Opportunities
- tgt-radaji's verify closes ~13 Aug: no failure mail for the week AND at least one "absorbed transient" line in `journalctl --user -u todoist-sync` (the new evidence path). Both present → close the outcome.
- Lauren's rollout review remains the only open clause on tgt-nefoja — still the natural experiment, no setup needed.
- banc-jicuzu then a targeted judge regrade of review-tier-c gives the convention cell trustworthy prose scores to sit beside the deterministic ones.
- A second convention cell — the mirror-aware edit (edits land on the canonical side of a synced pair) — is designed in tgt-sogude's brief but unbuilt; it needs a fixture-discoverable hint about which side is canonical, which is the hard design bit.

## For Claudes to come

Isolation comes in layers and only the kernel layer is security: ardoise's env -i wall strips context (CLAUDE.md, skills, plugins) but a process that wants the filesystem still has it — the real wall on tube is a second Unix user and a mode-700 home, with billing crossing as a scoped service-account key (model calls on one pot project, nothing else) rather than any broad credential. The pattern generalises: stage inputs across, sudo -n across, copy artifacts back, and let a falsifier script own the claim — kernel denial probes with a known-positive control, leak markers derived from the live subject at check time (never committed), a synthetic positive proving the grep can fire. Two pieces of instrument craft bought with this session's mistakes: a negative-result check counts only when something proves the subject actually ran (an auth failure passed both sabotage greps — error text contains "fail", and no model output means no leak markers, so the check glowed green over a run that never happened); and when a cell has a deterministic verdict, tell the LLM judge NOT to re-derive it — confine it to the register of the reasoning (strategic vs urgency-led), reserve 0.0 for named floors, or it will zero perfect runs for style while 1.0-ing beautiful wrong ones.
