The CLI is transitioning from single-user to team use. The SDK (todoist-api-python v4) doesn't wrap the `/api/v1/user` endpoint, so identity is resolved via a direct httpx call to the same REST v1 base URL. This is a seam — if Todoist changes the user endpoint, the SDK changelog won't flag it.

**Personal vs team filtering is settled.** The `workspace_id` field on the Todoist Project object is the correct signal — not `is_shared`, not collaborator count. Personal projects (`workspace_id=None`) show all tasks even with collaborators added; workspace projects (`workspace_id` set) auto-filter to the current user's assigned tasks, with `--unassigned` as the triage escape hatch for seeing the untriaged pool. "At Work" having collaborators but being functionally personal is exactly why collaborator count was the wrong heuristic.

**Data-model truths, live-verified 2026-08-02:** `created_at` in CLI output carries the API's `added_at` — there is no `added_at` key, and a jq probe of a missing key mints a null that reads like a measured value (`jq 'has("field")'` is the discriminator). A `move_task` resets the task's order to 1, so ordering always happens *after* moving. Clearing a section is done by moving a task to its own project's root — that is the API's mechanism, not a workaround. `reorder` is just `update_task(order=N)` per task; the SDK wraps `child_order`.

**Structural lesson:** `flatten.py` was broken for four months by a rename in `common.py` because no test imported it — four months of green CI over a dead entry point. `tests/test_imports.py` now smoke-imports every module and entry point; extend its MODULES list when adding a module.

The team is now using batterie, so generalisation is real, not theoretical. The skill has been rewritten user-agnostic — no hardcoded project names or team member details. The skill's frontmatter name is `coaching` (matching its directory), discovered as `accomplis:coaching`. **The skill teaches discovery over assertion** because even the original author's Todoist drifted from the skill's asserted examples (`@Wait` became `& Waiting For`): read a project's section names to tell outcomes-as-sections from status-lanes before querying, and match the discovered layout when writing.

**Residence beats richness (2026-08-05).** One always-loaded sentence (the bon shard's "past-tense verb + the so-what") outperformed 1,500 lines of unloaded skill across a full coaching session — the skill's MANDATORY-gate frontmatter never fired because the session entered through a data question, not a review request. When building skills: put the gate in the always-loaded shard, the depth in the skill, and never trust description-matching to route a session that's already moving. This is the core insight behind tgt-nefoja (reliable routing) and shapes tgt-vusipa's integration work.

**The GTD review is a repeatable product now (2026-08-05):** structure → altitude → language → arc → tier, with desire's two failure modes (phrasing gap vs dilution) needing opposite treatments. The full method lives in `docs/coaching-deltas-draft-2026-08-05.md` — drafted same-day by the session that lived it, resting on one week's evidence (two boards, one team survey); the team rollout sessions (Lauren → Alex → Judi → Ellie → Rupert → Ella) are the test suite and may contradict details.

**Draft hot, edit cold.** When handing off writing work, the session that lived the material drafts the prose (it can still feel what the spec omits); the fresh session gets the editorial pass. Reversed from a fresh-session-does-everything plan by Sameer's closing catch, and it's why the deltas draft exists alongside the tgt-nefoja spec.

**Methodology reference lessons** (GTD_METHODOLOGY.md): synthesised from FacileThings, NAA setup guide, and Sameer's 10-point induction doc — and the induction doc was the real quality gate. The framings that make GTD land: types not sizes, mental RAM, pressure release valve. Synthesising is harder than combining — the rewrite got shorter while adding content. GTD_METHODOLOGY.md deliberately contains no CLI commands: methodology and tool are separated on purpose.

**The weekly review is Allen's canonical sequence** — Get Clear (inboxes to zero) → Get Current (lists match reality) → Get Creative — agreed as of 2026-08-02 across all four surfaces: SKILL.md, GTD_METHODOLOGY.md, the "GTD Induction in 10 Points" Google Doc, and the induction speaker notes in Drive. This flipped from the earlier Peake ordering (Get Current first) on Sameer's call, after the 10-point doc was found quoting Peake's flip while implementing Allen's sequence with swapped labels. Peake's real contribution is the caution that reflection is the point — don't let processing crowd out the review — not a phase reordering.
