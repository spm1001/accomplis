# Handoff — 2026-08-02

session_id: (unresolved — cwd changed identity mid-session; evening session, tube, ~17:48–21:00)
purpose: Warmup: Bon↔Todoist GTD alignment pass; then the todoist-gtd → accomplis rename, shipped end-to-end
format: fond-v1

## For the next Claude

**You are in the repo formerly known as todoist-gtd.** The tool is now **accomplis** — repo `spm1001/accomplis`, plugin `accomplis@batterie`, commands `accomplis` / `accomplis-flatten`, suite 1.28.0.

### Done
- Alignment warmup (Sameer's framing: one next action per active project): mapped Projects - Toolmaking ↔ & Toolmaking ↔ the 44-board bon estate. All 8 bon-ID'd next actions resolved live. Fixed the four NA-less outcomes: minted NAs for Marmite (iw-sadabe) and BuJo→Carnet (carnet-japase); completed "Ship todoist GTD skill for team" with resolution in its description; homed "A meeting note starts itself" — first on carnet, then re-homed to notes (notes-cojemo + notes-ticati) after Sameer corrected the mechanics (both devices write-sync into ~/notes; the Shortcut is pure on-device, no server, carnet has no role). Promoted Areas of Focus to a project (Todoist outcome + notes-mecamo, first action notes-hasacu in Sameer's own words).
- Adjudicated (b) of the warmup: alignment mechanics live in bon:review's rebuild (bon-jagoha); todoist-gtd/accomplis stays team-generic; the projection endpoint is bon-wivuti's work-atom sketch.
- **tgt-depebe complete — the rename.** Everything renamed: package (`src/accomplis`), entry points, plugin, hook (`ensure-accomplis.sh`, which also retires a squatting todoist-gtd uv tool), GitHub repo, local dir. Service names deliberately kept: `TODOIST_API_KEY`, `todoist-api-key` Keychain service, `todoist-api-python` dep — zero re-auth on any machine. Token ladder gained its 4th rung, test-backed (29/29 incl. new ladder-order test). Suite 1.28.0 published, assemble green.
- Cross-repo sweep (all pushed): batterie-de-savoir (brigade.toml, regenerated tables, tools/accomplis.md with a truth-pass, publish/lint mappings, fixtures 34/34), dist repo (assemble.sh PLUGINS map + **hand-fixed marketplace.json** — see Risks), bon manifest.json, mit-commons toolkit shard (teammates now route to accomplis@batterie), trousse exemplars, infra tube ops doc + Marmite repo map, todoist-sync (repointed to `accomplis`, reinstalled, dry-run verified "twins in step", timer active), ~/.claude traps row (symlink escape).
- **Cutover complete on tube AND the Mac** (Mac over SSH: plugin swapped, CLI 1.28.0, registry verified 0×old/1×new). hezza deliberately exempt — it's mid-decommission (iw-limomu).
- Filed: bds-dekava (generate marketplace.json from vendored plugin.json — full design in brief), bon-rofatu (bon move parent-guard counts closed children, so its own remedy can't unblock; workaround recorded).
- SDK currency checked on Sameer's ask: locked at todoist-api-python 4.0.0 = PyPI latest (Mar 2026); dependabot.yml + uv Dependency Graph live and proven (last session's idna fix). Benign, no action.
- CHANGELOG rename entry added; GitHub repo description checked — already service-named, unchanged.

### Reflection
- The rename was safe because of one early decision: **split service names from tool names**. Everything that identifies the credential or the vendor kept `todoist`; everything that identifies our tool became `accomplis`. No machine re-authenticated.
- The consumer sweep caught **todoist-sync** — a systemd timer shelling out to the CLI every ~10 min. Cutover order mattered: new binary in → repoint → verify → old binary out. "Grep for what runs it" earned its keep.
- A vacuous audit nearly shipped: my exclusion grep carried `-i`, making the `Todoist` term swallow every case variant — residual check passed on an instrument that could not fail. Caught by re-reading the probe, not the result. (verification.md family 1, live instance.)
- Two shared-repo collisions handled: dist push rejected under a CI reassemble (fetch-before-write), and my `bon step` double-advance briefly marked step 3 done on intent rather than evidence.
- The permission gate stopped the GitHub rename on a musing ("maybe accomplis is sensible?") — correctly. Sameer's explicit yes resumed the flight. Good fence, right height.

### Uncertain
- macos-fluency claims `installed_plugins.json` is git-tracked in ~/.claude; on the Mac it is gitignored and untracked (add refused, tree clean). Stale note or machine divergence — watch, correct the skill if it recurs.
- `ensure-accomplis.sh` hasn't fired live yet (session-cached hooks). Expected silent: CLI present. First session start on each machine proves it.

### Risks
- **The symlink `~/repos/spm1001/todoist-gtd → accomplis` is still in place** — it held this session's recorded cwd. Remove it first thing next session here: `rm ~/repos/spm1001/todoist-gtd`. Until then, greps see a phantom second repo.
- **marketplace.json in the dist repo is hand-maintained** (assemble.sh rsync-excludes it; invariant is one-directional). Today's rename stranded the marketplace for ~30 min until hand-fixed. bds-dekava carries the generate-don't-check fix, ready to draw.
- Teammates with the old todoist-gtd@batterie plugin keep a working-but-orphaned install (no updates). New installs route correctly via the mit-commons shard. Whether to send a one-line team note is Sameer's call.
- Historical records (both CHANGELOGs' older entries, dated docs, trousse error-patterns, gueridon fixtures) deliberately still say todoist-gtd — they record what was true. Don't "fix" them.

### Opportunities
- `rm ~/repos/spm1001/todoist-gtd` — the symlink, 10 seconds, this repo, next session.
- tgt-muwuke: build the smevals eval (brief updated — the fixture shim is named `accomplis` now).
- bds-dekava (batterie-de-savoir's board): make the assembler generate marketplace.json — design complete in the brief.
- notes-hasacu (notes' board): the Areas of Focus first conversation Sameer queued — "AoFs for all my life, and how to reason about them."

## For Claudes to come

A suite-plugin rename touches, in order: the source repo's internals (package, entry points, plugin.json, hooks — and the hook should retire the old uv tool); the GitHub repo (redirects come free); brigade.toml + regenerated tables in batterie-de-savoir; assemble.sh's PLUGINS mapping in the dist repo; **the dist repo's marketplace.json, which is hand-maintained and rsync-excluded — the assembler will vendor the new plugin dir and happily leave the marketplace declaring the old one** (bds-dekava exists to kill this); the mit-commons toolkit shard; every *consumer* of the CLI (todoist-sync's systemd timer was the near-miss — grep the estate for what invokes the binary before retiring it); the token-store migration ladder (extend the source list, never reinvent — test-backed in token_store.py); and a per-machine plugin+CLI swap, where the Mac needs no token work at all because it uses the Keychain. The principle that made it low-drama: **service names and tool names are different names** — env vars, keychain services, and SDK deps identify the credential/vendor and survive; everything tool-named changes. And when a rename must land mid-session, `mv old new && ln -s new old` keeps the harness's recorded cwd resolving — now a traps.md row.
