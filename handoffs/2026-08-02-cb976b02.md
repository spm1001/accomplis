# Handoff — 2026-08-02

session_id: cb976b02-e879-40d9-a2bb-4d95db3f5da9
purpose: Fresh review + polish of the Skill/CLI package; field-report gaps shipped; weekly review flipped to canonical
format: fond-v1

## For the next Claude

### Done
- Fixed `todoist-flatten` — dead since dc9f928 (2026-03-30) with an ImportError under four months of green CI, because nothing imported it at test time. `tests/test_imports.py` now smoke-imports every module and entry point.
- Closed tgt-hiredu (all four reconciliation-session gaps): `update --no-section` (moves to project root — the API's only section-clearing mechanism), `update --order N` + `reorder ID...` verbs (live-verified on scratch projects; **a move resets order to 1**, so order applies after moves), created_at/added_at documented, skill rewritten to teach discovery over assertion.
- Gap 2 of that field report was disproven, not fixed: there is NO `added_at` key in CLI output — `created_at` carries the API's `added_at` and was always populated. The "added_at is null" finding was jq minting null for a missing key. `jq 'has("field")'` is the discriminator; documented in CLI_REFERENCE data model. Corpus candidate filed as carte-jutuce on ~/.claude's board.
- Closed tgt-kobale: token dir renamed to post-cutover `todoist-gtd-batterie` with test-backed migration from both older locations (`tests/test_token_store.py`). Swept tube/hezza/Mac: no husk dirs exist anywhere; tube+hezza still run on legacy `~/.todoist-token` and will migrate on first read.
- References swept: MIT-real examples → pungent-but-imaginary Australian market (OzTAM, Mediaweek, fictional product "SpotLift") on Sameer's steer; stale MCP references removed; obsolete "arc" vocabulary replaced; Sublime loop generalised; "Freedom to Be Curious" personal metric replaced with generic headroom check (completes the March decision).
- Weekly review flipped to Allen's canonical labels (Get Clear → Get Current → Get Creative, same activities) across FOUR surfaces, all now agreeing: SKILL.md, GTD_METHODOLOGY.md, the "GTD Induction in 10 Points" Google Doc, and gtd-induction-speaker-notes.md in Drive. Sameer's call, after the 10-point doc was found quoting Peake's flip but implementing Allen's sequence with swapped labels. Peake's aphorism reworded in both Drive files to the insight it carried ("reflection is the point — don't let processing crowd out the review").
- Shipped suite 1.27.0 (minor — new verbs), 1.27.1 (examples), 1.27.2 (flip); all assemble runs green; repo CI green throughout; idna Dependabot alert fixed in uv.lock. Also: Python floor aligned on 3.11, vestigial requirements.txt deleted, unused imports removed, doctor/auth fetch one page not all projects.
- Filed bon-wakaju on Bon's board: "Last session (Nd ago)" displays clone age not session age — open-context.sh selection uses the header date (with a comment naming the clone-flatten hazard) but the displayed age three lines later still reads file_mtime. This repo's July 6 re-clone (owner-bucketing) flattened all handoff mtimes to the same microsecond.
- Filed tgt-muwuke: smevals eval design for the coaching skill (fixture-shim todoist on PATH logging every argv; with/without-skill configs; two fixture worlds mismatching the skill's examples; ardoise isolation).

### Reflection
- The reviewing instinct that paid: read the substrate before the probe. The jq-null near-miss was survivable only because the SDK model had been read first — the same probe run cold had already fooled the previous session.
- The "big thumbs up" pattern: the reconciliation session's architecture verdict was right AND it filed one false gap AND missed a dead entry point. Fresh eyes plus a known-positive discipline caught both. Review quality is not additive with enthusiasm.
- Three publishes in one session (1.27.0/.1/.2) — each a coherent unit and user-sequenced, but worth a batching instinct next time.
- Sameer approved the flip and the direction, not my exact replacement wording in his two Drive files — worth his eyes on §9 next time he's in the doc.

### Risks
- The two Drive-file edits have no git. Restore points: the Google Doc's pre-edit Version history anchor is 2026-01-05 17:40 (revision 148); the speaker notes rely on Drive version history (plain file, three replace_texts on 2026-08-02 ~17:45).
- `reorder` touches only the tasks listed — unlisted siblings keep their orders and may interleave. If the Sunday Toolmaking ceremony finds the list-everything requirement clumsy, the designed extension is a `--before/--after` relative form.
- The coaching skill's frontmatter description changed (freedom-score trigger out, 'reorder my queue' in) — README table regenerated and CI-checked, but session-cached skill frontmatter means the new description only fires after restart.

### Uncertain
- Whether skill+cheap-model beats bare-frontier on the coaching tasks — tgt-muwuke's most interesting axis, unmeasured until the eval runs.

### Opportunities
- **Sameer has a warmup queued** — he said so at close; ask before picking anything else up.
- tgt-muwuke: build the smevals eval (design complete in the brief; `uv tool install smevals` to start).
- tgt-depebe: the kitchen rename, now carrying the migration-ladder addendum — do it before team adoption deepens; every new installed machine raises the cost.
- carte-jutuce (on ~/.claude's board): corpus decision on the jq-null row.

## For Claudes to come

The CLI's data-model truths, live-verified 2026-08-02: `created_at` carries the API's `added_at` (there is no `added_at` key — a jq probe of the missing key mints a null that reads as a measured value; `has()` discriminates); a `move_task` resets the task's order to 1, so ordering always happens after moving; clearing a section is done by moving a task to its own project's root — that is the API's mechanism, not a workaround; `reorder` is just `update_task(order=N)` per task, the SDK wraps `child_order`. The package's structural lesson: `flatten.py` was broken for four months by a rename in `common.py` because no test imported it — `tests/test_imports.py` now guards every module and entry point; extend its MODULES list when adding a module. The skill teaches discovery-over-assertion because even the original author's Todoist drifted from the skill's asserted examples (`@Wait` became `& Waiting For`): read a project's section names to tell outcomes-as-sections from status-lanes before querying, and match the discovered layout when writing. The weekly review is Allen's canonical Get Clear (inboxes to zero) → Get Current (lists match reality) → Get Creative, agreed across the skill, the methodology reference, and both Drive teaching docs — Peake's contribution is the caution that reflection is the point, not a phase reordering.
